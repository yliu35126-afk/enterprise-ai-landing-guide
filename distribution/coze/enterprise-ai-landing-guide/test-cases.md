# 扣子测试用例

1. API插件导入通过，不含内部统计或FDE管理路由。
2. 创建会话时固定归因 `COZE`。
3. 两个扣子对话的 `sessionId/sessionToken` 不串话。
4. 每轮只返回一个主要问题，第6轮或主动要求后可生成地图。
5. 未授权转换返回400，FDE无客户和商机写入。
6. 同意保存且不同意联系时转换成功，不创建联系人。
7. 相同幂等键重试不重复创建商机。
