identity:
  name: get_ai_landing_map
  author: blueprint-fde
  label:
    en_US: Get Ai Landing Map
    zh_Hans: 读取企业AI落地地图
description:
  human:
    en_US: 读取已生成的地图，不产生新写入。
    zh_Hans: 读取已生成的地图，不产生新写入。
  llm: 读取已生成的地图，不产生新写入。
parameters:
  - name: session_id
    type: string
    required: true
    label:
      en_US: 匿名会话ID
      zh_Hans: 匿名会话ID
    human_description:
      en_US: 使用创建会话工具返回的sessionId
      zh_Hans: 使用创建会话工具返回的sessionId
    llm_description: 使用创建会话工具返回的sessionId
    form: llm
  - name: session_token
    type: string
    required: true
    label:
      en_US: 短期会话Token
      zh_Hans: 短期会话Token
    human_description:
      en_US: 仅使用创建会话返回的sessionToken，不写入日志
      zh_Hans: 仅使用创建会话返回的sessionToken，不写入日志
    llm_description: 仅使用创建会话返回的sessionToken，不写入日志
    form: llm
extra:
  python:
    source: tools/get_ai_landing_map.py
